import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  settingsContainer: {
    flex: 1,
    backgroundColor: '#fff',
    justifyContent: "center",
    alignItems: "center",
  },
})